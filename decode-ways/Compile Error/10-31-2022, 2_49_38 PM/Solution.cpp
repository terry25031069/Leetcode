// https://leetcode.com/problems/decode-ways

class Solution {
public:
    int substrCombination(string s, int front){
        int case1 = (s[front] - '0');
        if(front < s.size() - 1)int case2 = case1 * 10 + (s[front + 1])
    }
    int numDecodings(string s) {
        substrCombination(s, 0)
    }
};